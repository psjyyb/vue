<template>
<h1>{{props.title }}</h1>
<h1>{{ props.subject }}</h1>

</template>
<script setup>
    import {defineProps} from 'vue';
    //let props = defineProps(['title','subject'])
    let props = defineProps({title:{type:String, default:'타이틀초기값'} ,
                subject:{type:String, required:true}})
</script>